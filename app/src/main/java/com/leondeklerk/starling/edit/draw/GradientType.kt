package com.leondeklerk.starling.edit.draw

/**
 * The different types of gradients.
 * Used to differentiate between value space size for HSV color attributes.
 */
enum class GradientType {
    HUE,
    SATURATION,
    VALUE
}
