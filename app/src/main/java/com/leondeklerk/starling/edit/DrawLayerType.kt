package com.leondeklerk.starling.edit

/**
 * Different types of available draw layers.
 * PAINT type is the generic marker/pencil/eraser drawings.
 * TEXT type is the text that is drawn over
 * Can be extended with stickers/emoji types.
 */
enum class DrawLayerType {
    PAINT,
    TEXT
}
